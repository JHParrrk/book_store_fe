export const getImgSrc = (id: number) => {
  return `https://picsum.photos/id/${id}/600/600`;
};

export const getThumbnailSrc = (id: number) => {
  return `https://picsum.photos/id/${id}/150/200`;
};
